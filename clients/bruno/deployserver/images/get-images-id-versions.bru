meta {
  name: List image versions
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/images/:id/versions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: uuid
}

headers {
  Accept: application/json
}

docs {
  Requires `image.read`.
}
