meta {
  name: Delete a site
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/sites/:name
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  name: string
}

headers {
  Accept: application/json
}

docs {
  Requires `machine.write`.
}
