meta {
  name: List sites
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/sites
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}

docs {
  Requires `machine.read`.
}
