meta {
  name: Get a job with its event trail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/jobs/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: uuid
}

headers {
  Accept: application/json
}

docs {
  Requires `job.read`.
}
