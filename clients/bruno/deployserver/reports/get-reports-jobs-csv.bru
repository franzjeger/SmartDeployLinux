meta {
  name: Flat CSV export of jobs in a window
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/reports/jobs.csv?since=7d
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  since: 7d
}

headers {
  Accept: application/json
}

docs {
  Requires `job.read`.
}
