meta {
  name: Delete a profile
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/profiles/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: uuid
}

headers {
  Accept: application/json
}

docs {
  Requires `image.write`.
}
